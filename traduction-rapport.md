# 报告中文翻译：猎物—捕食者生态系统模拟

## 1. 引言

本项目的目标是逐步构建一个由猎物和捕食者组成的简单生态系统模拟。第一步是在实现兔子的行为之前，先分析兔子的特征。

## 2. 练习 1 的目标

本练习要求识别能够描述一只兔子的信息，以及兔子将来可以执行的动作。这一分析为设计 `Lapin` 类做准备。

## 3. 问题回答

### 问题 1：属性

| 属性 | 含义 |
|---|---|
| `x` | 水平位置 |
| `y` | 垂直位置 |
| `energie` | 可用能量 |
| `age` | 兔子的年龄 |
| `vitesse` | 移动速度 |

### 问题 2：建议的方法

- `se_deplacer()`：修改兔子的位置；
- `vieillir()`：增加年龄并减少能量；
- `est_vivant()`：检查能量是否严格大于零。

移动、变老和存活判断将在后续练习中实现。

### 问题 3：UML 模型

对应的 UML 图位于 `docs/uml-exercice-1.md`。

## 4. 第一次实现

`Lapin` 类通过坐标初始化兔子。按照题目要求，默认值为：能量 100、年龄 0、速度 1。

```python
lapin = Lapin(10, 20)
print(lapin.x, lapin.y)
print(lapin.energie, lapin.age, lapin.vitesse)
```

## 5. 本阶段总结

本阶段帮助我们区分描述对象的数据和对象的行为。现在已经用一个最小类表示兔子；为了遵循 TP 的学习顺序，兔子的行为将在后续逐步添加。
