# 中文报告翻译：练习 1 至 6

## 1. 目标

第一阶段的目标是用一个简单的 Python 类表示兔子，然后操作多个兔子实例。

## 2. 兔子分析

| 属性 | 作用 |
|---|---|
| `x` | 水平位置 |
| `y` | 垂直位置 |
| `energie` | 可用能量 |
| `age` | 兔子的年龄 |
| `vitesse` | 移动速度 |

确定的动作是 `se_deplacer`（移动）、`vieillir`（变老）和 `est_vivant`（判断是否存活）。

## 3. 实现

构造函数接收 `x` 和 `y`。初始能量为 100，年龄为 0，速度为 1。

`se_deplacer(dx, dy)` 将位移加到坐标上。`vieillir()` 使年龄增加 1、能量减少 1。`est_vivant()` 在能量严格大于 0 时返回真。

## 4. 多个实例

创建五只兔子并放入一个列表，通过循环让所有对象变老。这样可以避免重复写五次代码，并且可以统一处理整个种群。

## 5. 验证

按照当前任务要求，单元测试直接放在 `lapin.py` 中，测试创建、移动、变老和死亡判断。

运行：

```bash
python lapin.py
```

## 6. 总结

练习 1 至 6 已完成，没有提前使用继承或其他类。直到后续步骤引入这些概念之前，当前设计保持简单。
